def two_pointers(arr):
    left = 0
    right = len(arr) - 1

    result = 0

    while left < right:
        # ==================================
        # This is where the problem-specific logic goes.
        # It's usually some 'if/else' block that decides
        # whether to move 'left' or 'right'.
        #
        # if (some_condition):
        #     left += 1
        # else:
        #     right -= 1
        # ==================================

    return result
